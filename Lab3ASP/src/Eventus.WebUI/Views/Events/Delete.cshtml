@model Eventus.WebUI.ViewModels.EventViewModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Event</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2" asp-display-name-for="@Model.Id"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.Id"></dd>

        <dt class="col-sm-2" asp-display-name-for="@Model.NameOfEvent"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.NameOfEvent"></dd>

        <dt class="col-sm-2" asp-display-name-for="@Model.CodeNumberOfService"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.CodeNumberOfService"></dd>

        <dt class="col-sm-2" asp-display-name-for="@Model.Description"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.Description"></"></dd>

        <dt class="col-sm-2" asp-display-name-for="@Model.EventDuration"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.EventDuration"></dd>

        <dt class="col-sm-2" asp-display-name-for="@Model.PriceOfTheEvent"></dt>
        <dd class="col-sm-10" asp-display-for="@Model.PriceOfTheEvent"></dd>
    </dl>

    <form asp-action="Delete" asp-route-id="@Model.Id">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Events">Back to List</a>
    </form>
</div>
